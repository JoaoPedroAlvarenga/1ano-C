#include<stdio.h>
int main(){
	int num,i,cont = 0;
		system("color a");
		printf("Informe um numero: ");
		scanf("%d",&num);
			
			while(cont < num){
				printf("%d\n",num);
				cont++;
			}
			
			for(i=0,i<=nun,i++){
				printf("%d\n",num);
			}
	
	system("pause");
	return 0;
}
