Program Pzim ;
uses crt;
var letra : char;
 Begin
  	write ('digite uma letra');
     read (letra);
 
     	if ((letra = 'a') or (letra = 'e') or (letra = 'i') or (letra = 'o') or (letra = 'u')) then
          	write('é uma vogal')
      else
          write('não é uma vogal')
  
  
 End.




















