Program Pzim ;
uses crt;
var letra : char;
var nome: String; 
 Begin
  	write ('digite seu nome');
     read (nome);
     
     if ((nome[1] = 'a') or (nome[1] = 'e') or (nome[1] = 'i') or (nome[1] = 'o') or (nome[1] = 'u')) then
		write('É volgal')
     else
		write('nao É volgal')
      
  
  
  
 End.
